# Meeting minutes

Record the meeting, then:

1. `wd transcribe meeting.wav --model base.en`
2. Export markdown with timestamps (`--format md`)
3. Paste into your notes app and tag with the project name

The markdown table keeps timestamps so you can jump back to the audio.
